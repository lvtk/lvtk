
image: registry.gitlab.com/lvtk/docker

x64_rel:
  stage: build
  image: registry.gitlab.com/lvtk/docker
  script:
    - meson setup build --native-file=meson/gitlab-ci.ini -Dbuildtype=release -Ddoc=disabled -Dtest=disabled
    - meson compile -C build
    - meson test -C build

osx:
  stage: build
  script:
    - meson setup build
    - meson compile -C build -j1
  tags: 
    - osx 
